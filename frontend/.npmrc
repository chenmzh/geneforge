# Allow esbuild's install script (required by Vite). pnpm 10+ blocks scripts by default.
only-built-dependencies[]=esbuild
# pnpm 10+ re-verifies the lockfile before every `pnpm run`; the esbuild script
# approval is environment-specific, so skip that gate for local builds.
verify-deps-before-run=false
